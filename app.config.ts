export default defineAppConfig({
  ui: {
    primary: "neutral",
    gray: "neutral",
    button: {
      rounded: "rounded-full transition-transform active:scale-[0.98]",
    },
    container: {
      constrained: "max-w-site",
      padding: "px-6 sm:px-8 lg:px-16",
    },
  },
});
